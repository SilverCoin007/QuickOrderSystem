@page "/"
<TopBar />
<ProductFilter OnCategorySelected="HandleCategorySelected" />
<ProductGallery SelectedCategoryId="@_selectedCategoryId" />

@code {
    private int _selectedCategoryId = -1;

    private void HandleCategorySelected(int categoryId)
    {
        _selectedCategoryId = categoryId;
    }
}